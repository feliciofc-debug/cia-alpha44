{
  "name": "@cia/api",
  "version": "0.1.0",
  "description": "Backend Fastify — pipeline de cotação CIA/Alpha 44.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json && node ../../packages/pipeline/scripts/copy-data.cjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@cia/db": "*",
    "@cia/fiscal-engine": "*",
    "@cia/pipeline": "*",
    "@cia/shared": "*",
    "@fastify/cors": "^10.0.1",
    "@fastify/multipart": "^9.0.1",
    "fastify": "^5.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
